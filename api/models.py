from django.db import models
# Ensure the correct model import in `api/serializers.py`
from bookings.models import Booking

# Remove or update any incorrect import statements in `api/serializers.py` or `api/models.py` like:
# from .models import Booking

# Create your models here.
